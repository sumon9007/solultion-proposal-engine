Alias command for exporting approved proposals.

## Instructions

1. Accept only approved proposal files as input
2. Use `python scripts/export_proposal.py <approved-proposal>`
3. Ensure exported output aligns with `docs/brand-guidelines.md`
4. Save rendered files only to `output/exports/`
